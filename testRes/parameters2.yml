name: Test
on: push
defaults:
  run:
    shell: sh
permissions:
  actions: write
  checks: write
  contents: read
  "id-token": read
jobs:
  Regression-Test-Stalkcd-Application:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo Hello World

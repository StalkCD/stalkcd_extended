pipeline {
    agent any

   node {
// HEADS UP: this is Scripted Pipeline syntax
    stage("Build") {
                build("Project testing")
                        build("Pipeline jenkins_pipeline_demo")

            }
    }

}
